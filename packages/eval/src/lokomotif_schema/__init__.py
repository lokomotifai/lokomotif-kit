"""Lokomotif Kit — Pydantic models for RTCSG modules.

The contents of this package (apart from this file) are AUTO-GENERATED by
`packages/schema/scripts/generate-python.ts`, which compiles the JSON
Schema into Pydantic v2 models via `datamodel-code-generator`.

Do not edit `module.py` by hand. Regenerate with:

    pnpm -F @lokomotif/schema generate:py

The generated module is committed; CI verifies the file is in sync with
the schema by running the generator and asserting `git diff --exit-code`.
"""

from __future__ import annotations

# When `module.py` is generated, re-export its public surface here.
# Until the first generation run lands, this package is a namespace
# placeholder — the import is intentionally guarded.
try:
    from .module import *  # noqa: F401,F403  (re-export of generated symbols)
except ImportError:  # pragma: no cover — generation has not yet run
    pass
